public Boolean isTypeAccount(sObject record)
{
    // code here
    List<sObject> lList = new List<sObject>();
    
    sObject acc = [SELECT Id FROM sObject LIMIT 1];
    
    if(record instanceOf sObject)){
        return true;
    }
    else {
        return false;
    }
}