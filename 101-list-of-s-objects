public List<sObject> getListOfsObject(List<Account> accounts, List<Contact> contacts)
{
    // code here
    List<sObject> lList = new List<sObject>();
    if(account.isEmpty() && contacts.isEmpty()){
        return lList.add('');
    }
    for(Account a : account){
        lList.add(a);
    }
    for(Contact c : contacts){
        lList.add(c);
    }
    return lList;
}