public List<String> getFieldsValue(Account acc, List<String> fields)
{
    // code here
    List<String> aList= new List<String>();
    for(String f:fields){
         String fieldValue = String.valueOf(acc.get(f));
         aList.add(fieldValue);
        }
    }
    return aList;
}