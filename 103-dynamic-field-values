public List<String> getFieldsValue(Account acc, List<String> fields)
{
    // code here
    List<String> aList= new List<String>();
    for(String f:fields){
        String fieldValue = acc?.get(f);
        aList.add(f);
    }
    return aList;
}